---
phase: 16.1-cross-surface-selection-rendering-keyboard-navigation
plan: 01
subsystem: ui
tags: [preact, webview, css-animation, cross-surface-sync, vscode-extension]

# Dependency graph
requires:
  - phase: 16-cross-surface-navigation-session-continuity
    provides: store.select() dedup, onDidSelect event, selection:highlight message contracts
provides:
  - selection:highlight message handlers in all 4 webviews (Hunt Overview, Evidence Board, Query Analysis, Drain Template Viewer)
  - hunt-selection-highlight and hunt-selection-pulse shared CSS classes
  - Evidence Board node:select emission on graph node click
  - Query Analysis store.select() propagation on query:select and receipt:select
  - Fixed TypeScript narrowing error in store.ts
affects: [16.1-02, keyboard-navigation, accessibility]

# Tech tracking
tech-stack:
  added: []
  patterns: [pulse-timer-ref pattern for 200ms CSS animation lifecycle in Preact]

key-files:
  created: []
  modified:
    - webview/shared/tokens.css
    - webview/hunt-overview/index.tsx
    - webview/evidence-board/index.tsx
    - webview/query-analysis/index.tsx
    - webview/query-analysis/app.tsx
    - webview/drain-template-viewer/index.tsx
    - webview/drain-template-viewer/app.tsx
    - src/queryAnalysisPanel.ts
    - src/store.ts

key-decisions:
  - "pulseTimerRef pattern: useRef + setTimeout(200ms) to toggle isPulsing state, avoiding CSS class removal race conditions across all 4 surfaces"
  - "Drain viewer cluster chips highlight when templateId OR parent queryId matches highlightedArtifactId"

patterns-established:
  - "Selection highlight pattern: useState<string|null> for highlightedArtifactId + isPulsing boolean + pulseTimerRef, shared across all webview surfaces"

requirements-completed: [XNAV-03]

# Metrics
duration: 6min
completed: 2026-04-03
---

# Phase 16.1 Plan 01: Cross-Surface Selection Rendering Summary

**End-to-end cross-surface selection highlighting wired into all 4 webviews with persistent outline + 200ms pulse animation, plus QA host store.select() propagation and store.ts TS narrowing fix**

## Performance

- **Duration:** 6 min
- **Started:** 2026-04-03T02:57:40Z
- **Completed:** 2026-04-03T03:04:19Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- All 4 webview surfaces (Hunt Overview, Evidence Board, Query Analysis, Drain Template Viewer) now handle `selection:highlight` host messages with visible CSS feedback
- Evidence Board graph node clicks emit `node:select` to host, enabling cross-surface selection propagation
- Query Analysis host panel calls `store.select()` for both query and receipt selections, completing the bidirectional sync loop
- Fixed TypeScript narrowing error in store.ts by extracting `hunt.state.data` to a local const before use in lambda

## Task Commits

Each task was committed atomically:

1. **Task 1: Add shared selection highlight CSS + selection:highlight handlers in all 4 webviews** - `2c08851` (feat)
2. **Task 2: Wire QA host store.select() calls + fix store.ts TS narrowing error** - `90444f1` (feat)

## Files Created/Modified
- `webview/shared/tokens.css` - Added hunt-selection-highlight, hunt-selection-pulse classes and @keyframes
- `webview/hunt-overview/index.tsx` - Added highlightedArtifactId state, selection:highlight handler, highlight on activity feed entries
- `webview/evidence-board/index.tsx` - Added highlightedArtifactId state, selection:highlight handler, node:select emission, highlight on graph nodes/matrix rows/columns
- `webview/query-analysis/index.tsx` - Added highlightedArtifactId state, selection:highlight handler, passes to App
- `webview/query-analysis/app.tsx` - Added highlightedArtifactId/isPulsing props to AppProps and ReceiptInspectorView, highlight on receipt list items
- `webview/drain-template-viewer/index.tsx` - Added highlightedArtifactId state, selection:highlight handler in message switch, passes to App
- `webview/drain-template-viewer/app.tsx` - Added highlightedArtifactId/isPulsing/queryId props to DrainChart, highlight on cluster chips
- `src/queryAnalysisPanel.ts` - Added store.select(msg.queryId) and store.select(msg.receiptId) calls
- `src/store.ts` - Fixed TS narrowing by extracting hunt.state.data to const stateData

## Decisions Made
- Used a consistent pulseTimerRef pattern across all 4 surfaces: useRef holds setTimeout handle, useEffect triggers on highlightedArtifactId change, clears previous timer to avoid race conditions
- Drain viewer cluster chips highlight when either the cluster templateId or the parent query queryId matches the highlightedArtifactId (broader matching for query-level selection)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Selection highlighting is fully wired end-to-end; Phase 16.1 Plan 02 (keyboard navigation / roving tabindex) can proceed
- All 4 surfaces have the state infrastructure for highlighting in place

## Self-Check: PASSED

All 9 modified files verified present. Both commit hashes (2c08851, 90444f1) verified in git log.

---
*Phase: 16.1-cross-surface-selection-rendering-keyboard-navigation*
*Completed: 2026-04-03*
