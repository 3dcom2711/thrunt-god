# Decision Log: THRUNT God VS Code Extension

Every contradiction across the four original specs and three critical reviews, resolved with rationale.

---

| # | Conflict | Resolution | Rationale |
|---|---------|------------|-----------|
| 1 | **Treemap vs stacked bar as primary Template Viewer visualization.** ARCHITECTURE.md uses treemap as primary (component named `TemplateTreemap.tsx`). UX-SPEC and VIZ-SPEC both recommend horizontal stacked bar. | **Horizontal stacked bar is primary.** Treemap deferred to v2 drill-down. | VIZ-SPEC scored four options across six criteria; stacked bar won decisively on cross-query comparison (5/5), VS Code fit (5/5), and interaction affordance (5/5). Hunter review confirms: "The horizontal stacked bar visualization is the right choice -- the designers correctly identified that cross-query comparison is the killer use case." |
| 2 | **Deviation score range: 0-5 vs 1-6 vs 0-10.** DATA-MODELS Zod schema uses `z.number().int().min(0).max(5)` for base and `z.number().int().min(0).max(10)` for composite. ARCHITECTURE.md uses 1-6 for both. VIZ-SPEC documents 1-6 scale with labels. | **0-6 for composite, with 0 meaning "no deviation / clear".** Base categories start at 1-3; modifiers add to composite; composite clamped to 0-6. | Actual receipt artifacts use score 0 (`EXPECTED_BENIGN -- score 0 (clear)`) and scores up to 6 (`composite deviation score of 6 (Critical)`). The Zod schema was wrong. The VIZ-SPEC labels (1=Minimal through 6=Critical) are correct for scored anomalies, but 0 exists for non-anomalous baselines. |
| 3 | **Deviation categories: 5 values vs 4 values.** ARCHITECTURE.md defines `EXPECTED_BENIGN | EXPECTED_MALICIOUS | AMBIGUOUS | UNEXPECTED_BENIGN | UNEXPECTED_MALICIOUS`. DATA-MODELS defines `EXPECTED_BENIGN | EXPECTED_MALICIOUS | AMBIGUOUS | NOVEL`. | **Three core categories from the source: `EXPECTED_BENIGN | EXPECTED_MALICIOUS | AMBIGUOUS`.** `NOVEL` added for cases with no prior baseline. | Actual receipt artifacts use only `EXPECTED_BENIGN` and `EXPECTED_MALICIOUS`. The `UNEXPECTED_BENIGN` and `UNEXPECTED_MALICIOUS` values from ARCHITECTURE.md never appear in any example artifact. `NOVEL` from DATA-MODELS is a reasonable addition for events with no established baseline, but `UNEXPECTED_*` is redundant with the modifier system. |
| 4 | **Hunt interface shape: flattened vs composed.** ARCHITECTURE.md flattens Mission fields into Hunt (`hunt.title`, `hunt.signal`). DATA-MODELS composes: `Hunt.mission: Mission`. | **Composition (DATA-MODELS approach).** `Hunt` holds `Mission` as a nested object. | Composition is correct. A Hunt contains a Mission, Hypotheses, HuntMap, etc. Flattening forces every consumer to know which fields come from which artifact. The composed shape maps directly to the file system. |
| 5 | **Hypothesis status casing: title-case vs lowercase.** ARCHITECTURE.md uses `'Active' | 'Supported' | 'Disproved'`. DATA-MODELS uses `'active' | 'supported' | 'disproved'`. | **Lowercase in code.** Parser lowercases on read from section headers. | The artifacts use title-case in Markdown headings, but internal representation should be normalized. Lowercase is idiomatic TypeScript for union types. The parser handles the conversion at the boundary. |
| 6 | **Phase status values: `'Active'` vs `'in_progress'`.** ARCHITECTURE.md uses `'Complete' | 'Active' | 'Pending'`. DATA-MODELS uses `'pending' | 'in_progress' | 'complete'`. | **`'pending' | 'in_progress' | 'complete'` (DATA-MODELS).** | `in_progress` is more precise than `Active` for representing a phase that has started but not finished. Lowercase for consistency with hypothesis status. |
| 7 | **`QueryLog` vs `Query` naming.** ARCHITECTURE.md calls it `QueryLog` and parser method `parseQuery` returns `QueryLog`. DATA-MODELS calls it `Query`. | **`Query`.** | Simpler, matches the artifact prefix `QRY-*`. `QueryLog` describes the format; `Query` describes the domain object. Every developer will type the short name. |
| 8 | **`AnomalyFraming` vs `AnomalyFrame` naming.** ARCHITECTURE.md uses `AnomalyFraming` with different internal structure. DATA-MODELS uses `AnomalyFrame`. | **`AnomalyFrame`** (DATA-MODELS naming). | More concise. The `-ing` suffix implies an action; the noun form better describes a data structure. Internal structure follows DATA-MODELS with named subtypes (`EntityBaseline`, `AnomalyPrediction`, `DeviationScore`). |
| 9 | **`dependsOn` type: `number[]` vs `string | null`.** ARCHITECTURE.md uses array of phase numbers. DATA-MODELS uses nullable string. | **`dependsOn: string | null`** for the raw parsed model. | The huntmap artifact uses `**Depends on:** Phase 1` as a single string. Parsing into `number[]` is a derived transformation. Add `dependsOnPhaseNumbers: number[]` as a computed field if graph edges need it. |
| 10 | **ClaimStatus values: 3 vs 4.** ARCHITECTURE.md omits `'context'`. DATA-MODELS includes `'supports' | 'context' | 'disproves' | 'inconclusive'`. | **Four values including `'context'`.** | The evidence graph edge types require `provides_context`. Receipts with `context` claim status exist in the evidence model. ARCHITECTURE.md's omission was an oversight. |
| 11 | **Receipt missing `title` field.** ARCHITECTURE.md Receipt has no `title`. DATA-MODELS has `title: string`. | **Include `title: string`.** | Receipt markdown files have an H1 title. Simple omission in ARCHITECTURE.md. |
| 12 | **QueryRuntimeMetadata: typed numerics vs raw strings.** ARCHITECTURE.md pre-parses into typed fields (`paginationLimit: number`, `warningCount: number`). DATA-MODELS preserves raw text (`pagination: string`, `warnings: string`). | **Typed numerics (ARCHITECTURE.md approach).** | The UI needs structured data for badges, sorting, and graph sizing. The parser converts strings to typed fields at parse time. Document the parsing rules. |
| 13 | **EvidenceReview sub-interface naming.** ARCHITECTURE.md: `QualityCheck`, `AntiPatternCheck`, `ClusteringIntegrityCheck`. DATA-MODELS: `EvidenceQualityCheck`, `EvidenceAntiPattern`, `TemplateClusteringCheck`. | **DATA-MODELS names** (prefixed, more explicit). | In a codebase with many check types, `EvidenceQualityCheck` is unambiguous. `QualityCheck` is too generic. Keep `valid` as `boolean` (parser converts "Yes"/"No"). |
| 14 | **`ClusteringIntegrityCheck.valid`: `boolean` vs `string`.** ARCHITECTURE.md uses `boolean`. DATA-MODELS uses `string`. | **`boolean`.** Parser converts "Yes"/"No"/"Pass"/"Fail" strings to boolean. | A boolean is more useful for programmatic checks and diagnostic generation. |
| 15 | **Webview message protocol: 4 generic messages vs 30+ granular messages.** ARCHITECTURE.md defines `init/update/highlight/theme-changed`. DATA-MODELS defines domain-typed messages for every operation. | **12 messages for v1** (trimmed from DATA-MODELS). See Part 4 of FINAL-DESIGN. | The generic protocol loses type safety on `Partial<T>` deltas. The granular protocol is over-engineered for three panels. The middle ground: one message type per panel concern, discriminated by `type` field. |
| 16 | **Store design: single-hunt vs multi-hunt, mixed state.** ARCHITECTURE.md: single `Hunt`, `invalidate()` method. DATA-MODELS: `hunts: Map<string, Hunt>`, mixed domain+UI state. | **Single hunt for v1.** Split into `HuntDataStore` (artifacts, indexes) and selection state (shared). | Multi-hunt support adds complexity to every operation. A hunter works one hunt at a time. UI state (selection, filters) is shared across panels via the store's selection field, not mixed into domain maps. |
| 17 | **Parser return types: bare domain objects vs `ParseResult<T>`.** ARCHITECTURE.md returns `Promise<Mission>`. DATA-MODELS wraps in `ParseResult<T>` with warnings and errors. | **`ParseResult<T>` for all parsers.** | Half-written artifacts (CLI mid-write) must not crash the extension. The error propagation path: parser returns `ParseResult<T>` -> store marks artifact state -> sidebar shows warning badge -> diagnostics surface parse errors. |
| 18 | **Evidence graph layout: force-directed vs Dagre hierarchical.** ARCHITECTURE.md uses d3-force with custom column pinning. UX-SPEC uses force-directed with hierarchical bias. VIZ-SPEC recommends Dagre. | **Dagre with `rankdir: 'TB'`.** | VIZ-SPEC argument is definitive: the evidence graph is inherently hierarchical (3 tiers). Dagre produces deterministic layouts. A force-directed graph with custom forces is a less reliable approximation. Hunter review confirms: "The dagre hierarchical layout is the correct choice. Force-directed would be actively harmful." |
| 19 | **Evidence graph node shapes: three conflicting specs.** ARCHITECTURE.md: hypothesis=rounded rect, query=diamond, receipt=hexagon. UX-SPEC: receipt=rectangle, query=cylinder. VIZ-SPEC: receipt=rectangle with 4px radius, query=small circle. | **VIZ-SPEC shapes** (most space-efficient). Hypothesis=rounded rect 180x60, receipt=rectangle 140-200x50 with corner radius, query=small circle 24px with connector icon. | Hexagons waste space. Diamonds are hard to label. The VIZ-SPEC shapes are the most practical for the constrained webview panel. Query nodes are small circles because they are Tier 2 (least important in the evidence chain). |
| 20 | **Evidence graph edge direction: arrows from child-to-parent vs parent-to-child.** UX-SPEC: Receipt->Hypothesis (child to parent). VIZ-SPEC: Hypothesis->Receipt->Query (parent to child, downward). ARCHITECTURE.md: both directions simultaneously. | **Arrows point downward: Hypothesis -> Receipt -> Query.** | Matches the visual hierarchy (hypotheses at top, queries at bottom). Matches the hunter's review flow: "I have a hypothesis, what supports it?" Edge labels carry semantic meaning (supports, contradicts, sourced-from). |
| 21 | **Template pinning storage: `.hunt/.thrunt-god/pinned-templates.json` vs `.hunt/STATE.md` vs `.hunt/.viz-state.json`.** All three write to `.hunt/`, violating the read-only architecture principle. | **VS Code `workspaceState`** (extension-managed key-value store). | The architecture's first design principle is "read-only by default -- the extension never writes to `.planning/`." Writing to `.hunt/` violates this. `workspaceState` persists per-workspace, requires zero filesystem writes, and survives VS Code restarts. |
| 22 | **Color palette: VS Code token-adjacent (UX-SPEC) vs Tailwind-derived (VIZ-SPEC).** Supported verdict: `#4EC9B0` (UX) vs `#22c55e` (VIZ). Disproved: `#CE9178` (UX) vs `#ef4444` (VIZ). | **UX-SPEC palette for sidebar/editor decorations. VIZ-SPEC palette for webview canvases.** Both registered as named color tokens in `contributes.colors`. | The UX-SPEC muted teal/orange blends with VS Code's native dark theme. Webview canvases need bolder colors against rendered backgrounds. Two palettes, clearly scoped. |
| 23 | **Deviation score colors: 4 buckets (UX-SPEC) vs 6-step gradient (VIZ-SPEC).** UX-SPEC: gray/yellow/orange/red for 1-2/3/4/5-6. VIZ-SPEC: green-400 through red-500, one per score. | **4 buckets for sidebar badges. 6-step gradient for webview visualizations.** | Sidebar badges have limited space -- 4 severity levels (Low/Medium/High/Critical) are sufficient. Webview visualizations benefit from finer granularity. |
| 24 | **Single-click behavior on evidence graph nodes: select vs open file.** UX-SPEC: single-click selects (shows detail panel). VIZ-SPEC: single-click opens `.hunt/HYPOTHESES.md`. ARCHITECTURE.md: single-click opens file, double-click opens webview. | **UX-SPEC pattern: single-click selects, double-click opens file.** | Matches VS Code's tree view conventions (single-click previews, double-click opens). Opening a file on single-click would be disruptive during graph exploration. The hunter needs to click multiple nodes to understand topology before committing to open a file. |
| 25 | **Keyboard shortcuts: `Ctrl+Shift+G/T/H` conflict with VS Code defaults.** UX-SPEC proposes shortcuts that collide with Source Control, Reopen Closed Editor, and Replace in Files. | **No default keybindings.** Register as chord suggestions: `Ctrl+K Ctrl+H` (sidebar), `Ctrl+K Ctrl+T` (template viewer), `Ctrl+K Ctrl+G` (evidence graph). | UX review correctly identifies all three conflicts. Chords are the VS Code convention for extension-specific shortcuts. |
| 26 | **Status bar: two items vs one.** UX-SPEC places hunt identity on left, integrity on right. UX review warns this is borderline and pushes native items. | **Single status bar item, left side, priority -100.** Combined format: `$(shield) OAuth Phishing | P3/3 | 2S 1D | OK`. | Consolidation follows Docker/Remote-SSH patterns. One item is less intrusive. Clicking opens a QuickPick with detailed status. |
| 27 | **Sparklines in TreeView sidebar.** VIZ-SPEC specifies 80x12px inline SVG sparklines in tree view descriptions. | **Drop sparklines from sidebar.** Use numeric deviation score badges via `TreeItem.iconPath` with SVG data URIs. | UX review identifies this as infeasible: `TreeItem.description` accepts only plain strings. Switching to WebviewView loses native tree behaviors (keyboard nav, collapse memory, context menus, accessibility). Sparklines can appear in webview panels but not the native tree. |
| 28 | **`.planning/` vs `.hunt/` directory resolution.** ARCHITECTURE.md watches both. DATA-MODELS uses `.hunt/` exclusively. Neither defines precedence when both exist. | **Resolution order: (1) `.hunt/MISSION.md` -> use `.hunt/`. (2) `.planning/MISSION.md` -> use `.planning/`. (3) Both exist -> QuickPick prompt. Setting `thruntGod.planningDirectory` defaults to `auto`.** | Architecture review section 4.3 identifies the ambiguity. The `auto` default handles most cases; the setting provides an escape hatch. Chosen root persists in `workspaceState`. |
| 29 | **Branded string types: 11 types for IDs.** DATA-MODELS defines `QueryId`, `ReceiptId`, `ManifestId`, `RequestId`, `HypothesisId`, `TemplateId`, `PackId`, `DetectionId`, `HeatmapId`, `ExecutionId`, `FeedbackId`, `DispatchId`. | **Keep 4 branded types for v1: `QueryId`, `ReceiptId`, `HypothesisId`, `TemplateId`.** Plain `string` for everything else. | Architecture review section 3.3: only these four are consumed by UI components. Branded types for unused IDs add noise without preventing bugs. Add others when they get consumers. |
| 30 | **Graph node types: 9 kinds.** DATA-MODELS defines hypothesis, query, receipt, finding, entity, template, pack, detection, phase. | **5 node types for v1: hypothesis, query, receipt, template, finding.** | Architecture review section 3.4: a graph with 9 heterogeneous node types is unusable. Entity nodes added in v2 as a toggle. Pack, detection, phase nodes cut entirely. |
| 31 | **Zod runtime validation.** DATA-MODELS requires Zod schemas for every domain model. | **Zod in tests only.** Production parsers use TypeScript types with null checks and fallback defaults. | Architecture review section 3.5: Zod adds ~14KB and validation overhead. The extension is read-only -- schema validation only catches malformed CLI output. Use Zod in test fixtures; use simple checks in production. |
| 32 | **Virtual scrolling via IntersectionObserver.** ARCHITECTURE.md proposes virtual scrolling for template table and timeline. | **Plain table for v1.** Add virtual scrolling if profiling shows lag. | Architecture review section 3.6: 50 templates is 50 rows. A basic HTML table handles this fine. Do not preemptively add complexity. |
| 33 | **Multi-hunt workspace support.** DATA-MODELS store holds `hunts: Map<string, Hunt>`. | **Single hunt for v1.** | Architecture review section 3.2: a hunter works one hunt at a time. Multi-root workspace support is v2. |
| 34 | **Scope creep: Manifest, Metrics, Feedback, Detection parsers.** DATA-MODELS defines parsers and types for MAN-*.json, HE-*.json, FB-*.json, DET-*.json. | **Cut all from v1.** | Architecture review section 2.5: none of these have corresponding UI panels or tree nodes. They are scope creep. |
| 35 | **Pack parsing and pack browser panel.** DATA-MODELS defines comprehensive pack data model (sections 1.17-1.18). | **Packs consumed indirectly through receipts only (`pack_progression_match`).** No full pack parser or panel in v1. | Architecture review section 2.6: ARCHITECTURE.md already takes this approach. The full pack model is not visible in any v1 UI. |
| 36 | **Receipt lineage model.** DATA-MODELS defines `ReceiptLineage` with replay fields. | **Cut from v1.** | Architecture review section 2.7: no consumer exists in sidebar, graph, or any panel. Add if replay queries become a common workflow. |
| 37 | **`ThemeColors` type: undefined vs `{ isDark: boolean }`.** ARCHITECTURE.md references `ThemeColors` in init message. DATA-MODELS uses `isDark` boolean. | **Pass `{ isDark: boolean }` in init. Webview reads CSS custom properties from `--vscode-*` variables.** | Architecture review section 2.9: this is the standard VS Code webview approach. The webview's `theme.ts` utility reads CSS variables at runtime. No need to serialize the full palette. |
| 38 | **Store change notification: `StoreChangeEvent` vs `changedKeys` subscription.** ARCHITECTURE.md fires `{ kind, affectedIds }`. DATA-MODELS fires `notify(changedKeys: Set<keyof HuntStore>)`. | **`StoreChangeEvent { kind, affectedIds }` (ARCHITECTURE.md approach).** | Architecture review section 2.10: kind + affectedIds enables targeted delta updates. Key-based notification forces subscribers to diff entire slices. |
| 39 | **WebviewPanelSerializer for v1.** ARCHITECTURE.md describes panel serialization for VS Code restart recovery. | **Cut for v1.** Panels re-opened manually after restart. | Architecture review section 6.7: serialization adds startup race conditions (panel restore before artifacts are parsed). Ship without it, iterate later. |
| 40 | **Observable Plot vs D3 for template viewer.** VIZ-SPEC recommends Observable Plot (~30KB). ARCHITECTURE.md uses D3 throughout. | **Observable Plot for stacked bar chart. D3 for evidence graph and timeline.** | Plot produces static SVG with a declarative API -- ideal for the simple bar chart. D3 is needed for the graph (custom node rendering) and timeline (Canvas + zoom/pan). Using both keeps bundle sizes proportionate to complexity. |
| 41 | **Evidence graph: full rebuild on any change.** Architecture review flags that GraphBuilder rebuilds entire graph on each file change. | **Incremental graph updates.** Send `graph:patch` with added/removed nodes and edges. | Architecture review section 5.1: for 160 nodes and 300 edges, full rebuild + serialize + re-render is wasteful. Incremental patches preserve D3 layout stability. |
| 42 | **Parser error handling: no error path in data flow.** ARCHITECTURE.md data flow goes straight from parser to store to UI with no error states. | **Every artifact has a state: `loaded | error | loading | missing`.** Store falls back to `lastGoodData` with stale indicator. Sidebar shows warning badges. | Architecture review section 4.1: half-written artifacts must not crash the extension. The `ArtifactState<T>` wrapper from the architecture review is adopted. |
| 43 | **CLI/extension race condition on partial file reads.** ARCHITECTURE.md proposes 300ms debounce, but debounce does not solve reading during multi-pass writes. | **Two mitigations: (1) Retry on incomplete parse (500ms, 2 retries). (2) Content-length stability check before parsing.** | Architecture review section 4.2: the CLI writes frontmatter, then body, then JSON blocks. A debounce may fire between passes. The retry + mtime stability check catches ongoing writes. |
| 44 | **Store update concurrency: parallel async parses.** Architecture review flags that two files changing simultaneously can produce stale indexes. | **Serialize store updates via a promise queue.** `this.updateQueue = this.updateQueue.then(() => this._processUpdate(kind, uri));` | Architecture review section 4.5: ensures index rebuilds always see the latest data from preceding updates. Simple, effective. |
| 45 | **`resultSummary`: `string` vs `QueryResultSummary`.** ARCHITECTURE.md stores raw string. DATA-MODELS stores typed object with numeric fields. | **`QueryResultSummary { events: number; templates: number; entities: number }`.** Parser converts at boundary. | Architecture review section 4.7: the UI needs structured data for badges and sorting. The parser splits on commas and equals, then parses integers. |
| 46 | **Activity bar icon visibility when no hunt exists.** UX-SPEC shows empty state sidebar. UX review says this confuses non-hunt users. | **Hide activity bar icon when no `.hunt/` or `.planning/` detected.** Use `"when": "thruntGod.huntDetected"` on the view container. | UX review section 4.2: follows the "lazy everything" architecture principle. The extension remains silent until relevant. |
| 47 | **Sidebar narrow-width handling.** UX review flags labels like `QRY-20260328-001 [4T]` truncating at 170-240px widths. | **Use `TreeItem.label` for short ID (`QRY-001`), `TreeItem.description` for metadata (`4T, 312 events`).** | UX review section 3.1: description renders right-aligned in dimmer color and truncates independently. Standard VS Code pattern. |
| 48 | **Template Viewer minimum width.** UX-SPEC specifies 400px minimum. UX review notes VS Code does not support minimum width constraints. | **Design for graceful degradation.** Below 400px: stack vertically. Below 300px: compact list (template name + count, no bar chart). | UX review section 3.5: the panel renders at whatever width the user drags. Define responsive breakpoints, not hard minimums. |
| 49 | **Evidence graph at scale (20+ nodes).** UX review identifies hairball problem at 50+ nodes. Neither spec addresses degradation. | **Focus mode default for graphs above 20 nodes.** Horizontal tab strip: `[HYP-01] [HYP-02] [All]`. One hypothesis subgraph at a time. | UX review section 2.3: progressive disclosure applied to the graph. "All" tab for overview seekers. |
| 50 | **Timeline swimlane explosion with many entities.** UX-SPEC: connector-entity pairs cause N*M lanes. | **Default grouping "By Entity" with connectors collapsed.** Shape/color distinguish connectors within lane. "Split by Connector" toggle expands selected entity. | UX review section 2.4: keeps default at N swimlanes (entities) rather than N*M (entities times connectors). |
| 51 | **Evidence graph glance value.** UX review rates graph glance value as "Fair" -- requires scanning node colors. | **Add summary strip above graph:** `3 Supported | 1 Disproved | 0 Inconclusive | Strongest chain: HYP-02 (Score 6)`. | UX review section 2.5: gives the graph the same status-at-a-glance as the status bar. |
| 52 | **Critical alert status bar behavior.** UX review identifies that highest-scoring anomalies are buried in the tree during active incidents. | **When any receipt has deviation score 5-6, status bar pulses:** `[!] RCT-001: Score 6 -- OAuth consent from Tor`. Click opens the receipt. | UX review section 2.1: takes the most important finding from "buried" to "one click." |
| 53 | **`HypothesisVerdict` casing in Findings.** ARCHITECTURE.md uses title-case `'Supported' | 'Disproved' | 'Inconclusive'`. DATA-MODELS `HypothesisVerdictRow` also uses title-case. General hypothesis status uses lowercase. | **Title-case for `HypothesisVerdict` in Findings (display-oriented).** Lowercase `HypothesisStatus` for internal hypothesis state. | Findings are a report artifact; title-case is appropriate for display. Internal state uses lowercase for consistency. These are different types for different contexts. |
| 54 | **SuccessCriteria parser: missing from ARCHITECTURE.md.** DATA-MODELS defines `SuccessCriteria`. ARCHITECTURE.md parser facade has no `parseSuccessCriteria()` method. | **Defer to v2.** Remove from `Hunt` interface for v1. | Architecture review section 2.3: success criteria are informational, not displayed in any v1 panel. |
| 55 | **EnvironmentMap parser: missing from ARCHITECTURE.md.** Same gap. | **Defer to v2.** Remove from `Hunt` interface for v1. | Architecture review section 2.4: environment data is not consumed by any v1 visualization. |
| 56 | **EntityNode: referenced but never defined.** DATA-MODELS references `entityIndex: Map<string, EntityNode>` but `EntityNode` is never specified. | **Define for v2 when entity nodes are added to graph.** For v1, entities are plain strings extracted from query entity timelines. | Architecture review section 2.8: entity nodes are a v2 graph feature. Defining the type now without a consumer adds dead code. |
| 57 | **Naming convention: camelCase vs snake_case in TypeScript interfaces.** Some interfaces mirror YAML snake_case (`query_id`); others use camelCase (`queryId`). | **camelCase in TypeScript. snake_case in YAML/JSON.** Parsers convert at boundary. | Architecture review section 6.5: standard practice. Zod schemas validate raw snake_case, transform step converts to camelCase domain objects. |
| 58 | **`HuntmapPhase.status` casing inconsistency.** ARCHITECTURE: `'Complete' | 'Active' | 'Pending'`. `HuntmapPlan.status` in same doc uses lowercase `'complete' | 'active' | 'pending'`. | **All lowercase.** | Internal consistency. Phase status and plan status should follow the same convention. |
| 59 | **Template Viewer: pinned templates section layout.** UX review identifies information overload with 4-5 simultaneous zones. | **Pinned section collapsed by default** with badge: "3 pinned" + chevron. Comparison table appears only on Ctrl+Click, not persistently. Detail pane is slide-over, not permanent. | UX review section 2.2: reclaims vertical space for the bar chart. |
| 60 | **VIZ-SPEC library choice: Observable Plot for stacked bar.** VIZ-SPEC recommends Observable Plot. ARCHITECTURE.md uses D3 for everything. | **Observable Plot for template viewer.** D3 for graph and timeline. | Observable Plot produces static SVG with 3 lines of code for a stacked bar. D3 would require 50+ lines for the same result. Right tool for the right job. |
| 61 | **Template-to-timeline event linkage: undefined join key.** Architecture review flags that `event_ids` and entity timeline `index` may use different namespaces. | **Join on `NormalizedEvent.id`.** Entity timeline events reference the same IDs via the `id` field. Document this explicitly. | Architecture review section 4.6: the join must be by normalized event ID, not by timeline row index. Parser must extract event IDs from entity timeline tables. |
| 62 | **Stale data indicator for webview.** Neither doc addresses what the user sees during re-parse. | **Send `{ type: 'stale', affectedIds }` immediately on invalidation.** Webview dims affected elements at 50% opacity until update arrives. | Architecture review section 6.6: gives visual feedback that re-parse is in progress without blocking interaction. |
| 63 | **Webview accessibility: ARIA roles.** Neither spec addresses ARIA for D3-rendered SVG/Canvas. | **Add ARIA roles to all interactive SVG elements.** Canvas timeline provides parallel accessible data table. | UX review section 5.2: screen readers cannot navigate into webview iframes using standard tree navigation. Each interactive element needs proper ARIA attributes. |
| 64 | **Webview focus trap.** UX review notes VS Code shortcuts do not work inside webview focus. | **Implement Escape -> `vscode.postMessage({ type: 'blur' })` -> extension host focuses editor group.** | UX review section 5.3: standard mitigation for VS Code webview focus traps. |
| 65 | **High Contrast theme for VIZ-SPEC deviation gradient.** VIZ-SPEC does not address High Contrast. Yellow-400 against white fails AA contrast. | **Define High Contrast variants.** Switch palettes based on `window.activeColorTheme.kind`. Yellow -> amber-700 for HC Light. | UX review section 5.4: ensures WCAG compliance across all VS Code theme types. |
| 66 | **`THRUNT: Run Phase` -- passive during execution.** Hunter review: "While the CLI is running, the sidebar shows spinning icon but I cannot see progress." | **Deferred to v2** (requires CLI integration). For v1, the extension is a viewer. Running phases requires the terminal. | Hunter explicitly rates this as friction, but bridging CLI output to the extension requires IPC design beyond v1 scope. |
| 67 | **Read-only constraint long-term.** Hunter review: "After a week, I will want to edit things from the extension." | **v1 is read-only.** v2 adds inline editing of `## Notes` sections and hypothesis status toggles. | Hunter feedback is valid but scope-expanding. Read-only keeps v1 simple and reliable. Notes editing is the highest-value write operation for v2. |
| 68 | **Search across artifacts.** Hunter review: "No way to search within the extension." | **Deferred to v2.** `THRUNT: Search Hunt` command that searches across all artifact text. | Important but not v1 scope. `Ctrl+Shift+F` global search works as a workaround. |
